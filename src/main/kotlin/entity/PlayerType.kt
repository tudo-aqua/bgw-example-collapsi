package entity

/**
 * Enum class that represents the type of a [Player] in the Collapsi game.
 * It can be a local player, a remote player, or a bot with different difficulty levels.
 */
enum class PlayerType {
    LOCAL,

    REMOTE,

    BOT
}